[Unit]
Description=Daily Ingestion Sync (Reading Analytics + Second Brain)

[Timer]
# Run once daily at midnight
OnCalendar=*-*-* 00:00:00
# Ensure catch-up if the machine was off
Persistent=true
# Prevent thundering herd (30m window)
RandomizedDelaySec=1800

[Install]
WantedBy=timers.target
